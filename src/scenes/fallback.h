#ifndef FALLBACK_H
#define FALLBACK_H

#include "../game/context.h"

#define FALLBACK_BTN_WIDTH 200
#define FALLBACK_BTN_HEIGHT 50
#define FALLBACK_BTN_OFFSET_Y 100

#define FALLBACK_FONT_SIZE 48

void run_fallback_scene(GameContext *ctx);

#endif
